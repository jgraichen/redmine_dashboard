<td class="dashboard-<%=column.id%>" id="<%='drop-'+group.id+'-'+column.id%>">
<% if issues.length > 0 -%>
  <div class="dashboard-count">
    <span><%= l(:label_x_issues, :count => issues.length) %></span>
  </div>
<% end -%>
<% issues.each do |issue| -%>
  <div class="dashboard-issue drag-<%=group.id%> hascontextmenu" id="issue-<%=issue.id%>">
<%= render :partial => DashboardController::VIEW_MODES[@view], 
           :locals => {
           :issue => issue
    } %>
  </div>
  <script type="text/javascript"><%= 
    draggable_element_js(
      "issue-"+issue.id.to_s, 
      :scroll => :window, 
      :revert => :true,
      :onEnd => "function(obj){obj.element.setStyle({top:0,left:0});}"
    ) if @edit_enabled %></script>
<% end -%>
</td>