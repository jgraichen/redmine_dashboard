%ul.rdb-headers
  - @board.columns.each_value do |column|
    %li.rdb-column{:class => column.hide? ? 'rdb-column-hide' : ''}
      %h3= column.title
      - if column.issues.count > 0
        %span= t :rdb_x_issues, :count => column.issues.count
